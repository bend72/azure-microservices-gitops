# ── helm/services/identity/Chart.yaml ────────────────────────────────────────
apiVersion: v2
name: identity
description: Identity microservice — authentication, token issuance
type: application
version: 0.1.0
appVersion: "0.0.0"   # overwritten by Image Updater / CI
dependencies: []
