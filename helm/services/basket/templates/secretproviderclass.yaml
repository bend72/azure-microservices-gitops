# ── helm/services/basket/templates/secretproviderclass.yaml ──────────────────
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: {{ include "basket.fullname" . }}-kv
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "basket.labels" . | nindent 4 }}
spec:
  provider: azure
  parameters:
    usePodIdentity:        "false"
    useVMManagedIdentity:  "false"
    clientID:              {{ .Values.global.workloadIdentityClientId | quote }}
    keyvaultName:          kv-stage2-demo
    cloudName:             AzurePublicCloud
    tenantId:              TENANT_ID
    objects: |
      array:
        - |
          objectName: cosmos-basket-connection-string
          objectType: secret
        - |
          objectName: servicebus-connection-string
          objectType: secret
  secretObjects:
    - secretName: {{ include "basket.fullname" . }}-secrets
      type: Opaque
      data:
        - objectName: cosmos-basket-connection-string
          key: cosmos-basket-connection-string
        - objectName: servicebus-connection-string
          key: servicebus-connection-string

---
