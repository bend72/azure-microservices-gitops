# ── helm/services/payment/templates/secretproviderclass.yaml ─────────────────
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: {{ include "payment.fullname" . }}-kv
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "payment.labels" . | nindent 4 }}
spec:
  provider: azure
  parameters:
    usePodIdentity:        "false"
    useVMManagedIdentity:  "false"
    clientID:              {{ .Values.global.workloadIdentityClientId | quote }}
    keyvaultName:          {{ required "azure.keyVaultName must be set in values" .Values.azure.keyVaultName | quote }}
    cloudName:             AzurePublicCloud
    tenantId:              {{ required "azure.tenantId must be set — run: az account show --query tenantId -o tsv" .Values.azure.tenantId | quote }}
    objects: |
      array:
        - |
          objectName: sql-payment-connection-string
          objectType: secret
        - |
          objectName: servicebus-connection-string
          objectType: secret
  secretObjects:
    - secretName: {{ include "payment.fullname" . }}-secrets
      type: Opaque
      data:
        - objectName: sql-payment-connection-string
          key: sql-payment-connection-string
        - objectName: servicebus-connection-string
          key: servicebus-connection-string

---
